---
phase: 02-stats-scraper
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [src/scrapers/greyhound_stats_scraper.py, src/storage/dog_stats.py]
---

<objective>
Implement greyhound statistics scraper and database storage layer.

Purpose: Build production scraper that extracts dog statistics from greyhoundstats.co.uk and stores them in the database, following the data access strategy from Plan 02-01.
Output: Working GreyhoundStatsScraper class and database storage functions ready for batch execution.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-stats-scraper/02-DISCOVERY.md
@.planning/phases/02-stats-scraper/02-01-SUMMARY.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/scrapers/base_scraper.py
@src/models/dog.py
@src/storage/db.py
@src/utils/rate_limiter.py
@research_notes.md

**From Plan 02-01:**
- Data access strategy: [will be specified in 02-01-SUMMARY.md]
- URL patterns and CSS selectors documented
- Field mapping defined
- Scraping scope decided

**From Phase 1 RESEARCH - Don't Hand-Roll:**
- Anti-bot evasion: Use playwright-stealth (already in BaseScraper)
- Rate limiting: Use TokenBucketRateLimiter (already available)
- Retry logic: Use tenacity decorator (already in BaseScraper)

**Tech stack available:**
- BaseScraper (Playwright + stealth + rate limit + retry)
- BeautifulSoup for HTML parsing
- psycopg2 for database
- Dog model with JSONB stats field
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GreyhoundStatsScraper class</name>
  <files>src/scrapers/greyhound_stats_scraper.py</files>
  <action>Create GreyhoundStatsScraper that subclasses BaseScraper (from Phase 1). Implement parse() method using BeautifulSoup to extract dog statistics based on research_notes.md specifications. Extract fields according to documented CSS selectors: dog name, rating, win rate, recent form, track preferences, distance preferences, trap stats. Return structured dict matching Dog model JSONB stats format from 02-DISCOVERY.md. Handle missing data gracefully (use None/null for missing fields, don't crash). Include comprehensive docstring explaining what data is extracted and format. Import BeautifulSoup, BaseScraper, and TokenBucketRateLimiter. Constructor takes dog_url and rate_limiter. Use async/await pattern consistently (BaseScraper is async).</action>
  <verify>python -c "from src.scrapers.greyhound_stats_scraper import GreyhoundStatsScraper; print('Import successful')" succeeds</verify>
  <done>GreyhoundStatsScraper class implemented with parse() method, handles missing data, returns correct format</done>
</task>

<task type="auto">
  <name>Task 2: Create database storage layer for dog stats</name>
  <files>src/storage/dog_stats.py</files>
  <action>Create dog_stats.py with functions for storing and retrieving dog statistics. Implement upsert_dog_stats(dog_name, stats_dict) that inserts new dog or updates existing dog's stats JSONB field and last_stats_update timestamp. Use psycopg2 parameterized queries (from Phase 1 db.py patterns) with INSERT ... ON CONFLICT (dog_name) DO UPDATE pattern for PostgreSQL upsert. Implement get_dog_stats(dog_name) for retrieval. Implement list_dogs_by_last_update(limit) to find stale dogs. Use connection pooling from src/storage/db.py. Include error handling with try/except for psycopg2.Error. Add docstrings explaining usage. Note: Dog model race_id can be NULL for dogs not yet in a race (stats-only records).</action>
  <verify>python -c "from src.storage.dog_stats import upsert_dog_stats, get_dog_stats; print('Import successful')" succeeds</verify>
  <done>Database functions implemented with upsert, retrieval, and error handling</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test script</name>
  <files>test_stats_scraper.py</files>
  <action>Create test_stats_scraper.py that tests the complete pipeline: (1) Initialize TokenBucketRateLimiter with 0.5 rate (2-sec delays) (2) Create GreyhoundStatsScraper with a sample dog URL from research_notes.md (3) Run scraper and print extracted stats (4) Call upsert_dog_stats to store in database (5) Retrieve with get_dog_stats and verify (6) Print success message with stats summary. Use asyncio.run() for async execution. Include try/except to catch and display any errors clearly. This validates end-to-end functionality before batch job in Plan 02-03.</action>
  <verify>File exists at test_stats_scraper.py</verify>
  <done>Test script created that validates scraping -> storage -> retrieval pipeline</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dog stats scraper with database storage</what-built>
  <how-to-verify>
    1. Ensure PostgreSQL database is running and DATABASE_URL is set in .env
    2. Run migrations if not done: python -m src.storage.migrations
    3. Run test: python test_stats_scraper.py
    4. Expected: Script prints dog statistics, stores to DB, retrieves successfully
    5. Check for errors in scraping, parsing, or database operations
    6. Verify rate limiting (should take 2+ seconds per request)
  </how-to-verify>
  <resume-signal>Type "approved" if test passes, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GreyhoundStatsScraper class imports without errors
- [ ] dog_stats.py functions import without errors
- [ ] Test script runs successfully
- [ ] Data correctly stored in database
- [ ] Rate limiting observed (2-second delays)
- [ ] No errors or crashes on missing data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User approved test results
- Scraper handles missing data gracefully
- Database upsert works correctly
- Ready for batch job implementation (Plan 02-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stats-scraper/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Dog Stats Scraper Implementation Summary

**[One-liner: scraper implementation with storage layer complete]**

## Accomplishments

- Implemented GreyhoundStatsScraper subclassing BaseScraper
- Created database storage layer with upsert functionality
- Built end-to-end test validating scrape -> store -> retrieve
- Handled missing data edge cases

## Files Created/Modified

- `src/scrapers/greyhound_stats_scraper.py` - Dog stats scraper
- `src/storage/dog_stats.py` - Database storage functions
- `test_stats_scraper.py` - Integration test script

## Decisions Made

- Upsert strategy: INSERT ... ON CONFLICT for PostgreSQL
- Missing data handling: [approach used]
- Stats-only dogs: race_id can be NULL

## Issues Encountered

[Any parsing challenges, database issues, or data quality problems]

## Next Step

Ready for 02-03-PLAN.md (Batch Scraping Job)
</output>
