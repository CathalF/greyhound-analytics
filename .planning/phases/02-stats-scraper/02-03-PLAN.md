---
phase: 02-stats-scraper
plan: 03
type: execute
depends_on: ["02-02"]
files_modified: [src/batch_scraper.py, .gitignore]
---

<objective>
Create batch scraping job with logging and error handling for daily stats updates.

Purpose: Automate daily scraping of dog statistics at scale, with proper logging, error recovery, and progress tracking.
Output: Production-ready batch job that can scrape multiple dogs, handle failures gracefully, and log results for monitoring.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-stats-scraper/02-DISCOVERY.md
@.planning/phases/02-stats-scraper/02-01-SUMMARY.md
@.planning/phases/02-stats-scraper/02-02-SUMMARY.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/scrapers/greyhound_stats_scraper.py
@src/storage/dog_stats.py
@src/utils/rate_limiter.py
@research_notes.md

**From Plan 02-01:**
- Scraping scope: [N dogs to target]
- Data access strategy: [approach chosen]

**From Plan 02-02:**
- GreyhoundStatsScraper working
- Database storage validated
- Single-dog scraping tested

**Core value from PROJECT.md:**
"Accurate and reliable data scraping from both sources without errors or missing data"

**Constraints:**
- Must respect rate limits (2-5 second delays)
- Must handle failures gracefully (some dogs may have incomplete data)
- Must be runnable daily as batch job
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch scraping orchestrator</name>
  <files>src/batch_scraper.py</files>
  <action>Create batch_scraper.py with async main() function that orchestrates multi-dog scraping. Implement: (1) Read list of dog URLs from research_notes.md strategy (either from listing page discovery or predefined list) (2) Create single TokenBucketRateLimiter(rate=0.5) shared across all scrapers (respects 2-sec global rate limit) (3) For each dog URL, create GreyhoundStatsScraper and scrape (4) On success: call upsert_dog_stats, log success (5) On failure: log error with dog URL and exception, continue to next dog (don't crash entire batch) (6) Track stats: total dogs, successful, failed, duration (7) Print summary at end. Use asyncio.gather with return_exceptions=True to handle failures gracefully. Add --limit flag to scrape first N dogs (useful for testing). Include comprehensive logging with timestamps. Make it runnable: python -m src.batch_scraper</action>
  <verify>python -m src.batch_scraper --limit 5 runs without crashing, processes 5 dogs with rate limiting</verify>
  <done>Batch scraper handles multiple dogs, continues on errors, respects rate limits, logs results</done>
</task>

<task type="auto">
  <name>Task 2: Add structured logging</name>
  <files>src/batch_scraper.py</files>
  <action>Enhance batch_scraper.py with Python logging module (not print statements). Configure logging to: (1) Write to logs/batch_scraper.log with daily rotation (2) Format: timestamp, level, message (3) Log levels: INFO for progress, WARNING for missing data, ERROR for failures (4) Console output: INFO and above (5) File output: DEBUG and above for detailed troubleshooting. Create logs/ directory if doesn't exist. Use logging.basicConfig with RotatingFileHandler. Add logs/ to .gitignore (don't commit log files). Log: batch start/end, each dog processed, success/failure, final summary statistics.</action>
  <verify>logs/batch_scraper.log exists after run with structured entries, logs/ in .gitignore</verify>
  <done>Structured logging implemented with file rotation and appropriate levels</done>
</task>

<task type="auto">
  <name>Task 3: Add progress tracking and resume capability</name>
  <files>src/batch_scraper.py</files>
  <action>Add progress tracking to batch scraper: (1) Save progress to .scraper_progress.json after each successful dog (dog_name, timestamp, success/fail) (2) On startup, read progress file and skip already-processed dogs from today (based on timestamp) (3) This enables resume if batch job crashes mid-run (4) Add --force flag to ignore progress and re-scrape all (5) Log resume behavior: "Resuming from dog #X" or "Starting fresh batch". Use JSON format: {"date": "2026-01-13", "processed": ["dog1", "dog2"], "last_updated": "timestamp"}. Add .scraper_progress.json to .gitignore.</action>
  <verify>Run batch twice - second run skips already-processed dogs (unless --force used)</verify>
  <done>Progress tracking allows resume after failures, prevents duplicate work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Batch scraper processes multiple dogs
- [ ] Rate limiting respected (2-second delays observed)
- [ ] Failures don't crash entire batch
- [ ] Logs written to logs/batch_scraper.log
- [ ] Progress saved to .scraper_progress.json
- [ ] Resume functionality works
- [ ] --limit and --force flags work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Batch scraper ready for daily production use
- Error handling robust (continues on failures)
- Logging provides clear visibility into scraping process
- Phase 2 complete: Dog stats database operational
</success_criteria>

<output>
After completion, create `.planning/phases/02-stats-scraper/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Batch Scraping Job Summary

**[One-liner: production batch job with logging and resume capability]**

## Accomplishments

- Created batch orchestrator for multi-dog scraping
- Implemented structured logging with file rotation
- Added progress tracking and resume capability
- Built production-ready daily batch job

## Files Created/Modified

- `src/batch_scraper.py` - Batch scraping orchestrator
- `.gitignore` - Added logs/ and .scraper_progress.json
- `logs/batch_scraper.log` - Generated by batch runs

## Decisions Made

- Shared rate limiter across all scrapers (global 2-sec limit)
- Resume strategy: progress file tracks today's work
- Logging: INFO to console, DEBUG to file
- Failure handling: continue on errors, log and move to next dog

## Issues Encountered

[Any performance issues, error patterns, or data quality problems]

## Phase 2 Complete

**What we built:**
- Dog statistics database from greyhoundstats.co.uk
- GreyhoundStatsScraper with Playwright + stealth
- Database storage with upsert functionality
- Production batch job with logging and resume

**Ready for Phase 3:**
- Phase 3 will scrape oddschecker for race fixtures + odds
- Dog names from oddschecker will be matched against this stats database
- Value bets identified by comparing stats vs odds

**Usage:**
```bash
# Run full batch
python -m src.batch_scraper

# Test with first 10 dogs
python -m src.batch_scraper --limit 10

# Force re-scrape (ignore progress)
python -m src.batch_scraper --force
```

**Next Phase:**
Ready for Phase 3: Odds Scraper (oddschecker.com)
</output>
