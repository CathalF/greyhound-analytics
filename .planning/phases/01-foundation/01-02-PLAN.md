---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/models/race.py, src/models/dog.py, src/models/odds.py, src/storage/schema.sql, src/storage/migrations.py]
---

<objective>
Create data models and database schema for races, dogs, and odds.

Purpose: Define the core data structures that will store scraped information from greyhoundstats.co.uk and oddschecker.
Output: Python data models (dataclasses), PostgreSQL schema, and migration system ready for data storage.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@src/storage/db.py

**Requirements from PROJECT.md:**
- Track greyhound statistics (win rates, track preferences, recent form)
- Track live odds from oddschecker
- Historical race tracking for pattern analysis

**Data refresh patterns:**
- Stats: daily batch updates
- Odds: real-time (every few minutes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data models</name>
  <files>src/models/race.py, src/models/dog.py, src/models/odds.py</files>
  <action>Create Python dataclasses for core entities.

Race model (race.py): race_id (str), track_name (str), race_time (datetime), distance (int meters), status (str: scheduled/in_progress/completed), created_at, updated_at.

Dog model (dog.py): dog_id (str), name (str), race_id (str FK), trap_number (int 1-6), stats (dict for JSON - win_rate, recent_form, track_preference), last_stats_update (datetime), created_at.

Odds model (odds.py): odds_id (str), race_id (str FK), dog_id (str FK), bookmaker (str), decimal_odds (float), fractional_odds (str), timestamp (datetime - when odds were scraped), created_at.

Use @dataclass decorator, include type hints, add from_dict() and to_dict() methods for easy serialization. Import datetime from datetime module.</action>
  <verify>python -c "from src.models.race import Race; from src.models.dog import Dog; from src.models.odds import Odds; print('Models imported successfully')"</verify>
  <done>Three dataclass models created with all required fields, type hints, and serialization methods</done>
</task>

<task type="auto">
  <name>Task 2: Create database schema</name>
  <files>src/storage/schema.sql</files>
  <action>Create PostgreSQL DDL for three tables matching the data models:

races table: race_id VARCHAR(100) PRIMARY KEY, track_name VARCHAR(255) NOT NULL, race_time TIMESTAMP NOT NULL, distance INTEGER, status VARCHAR(50), created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW(). Add index on race_time for time-based queries.

dogs table: dog_id VARCHAR(100) PRIMARY KEY, name VARCHAR(255) NOT NULL, race_id VARCHAR(100) REFERENCES races(race_id) ON DELETE CASCADE, trap_number INTEGER CHECK (trap_number BETWEEN 1 AND 6), stats JSONB (PostgreSQL JSON type for flexible stats storage), last_stats_update TIMESTAMP, created_at TIMESTAMP DEFAULT NOW(). Add index on race_id for joins.

odds table: odds_id VARCHAR(100) PRIMARY KEY, race_id VARCHAR(100) REFERENCES races(race_id) ON DELETE CASCADE, dog_id VARCHAR(100) REFERENCES dogs(dog_id) ON DELETE CASCADE, bookmaker VARCHAR(100), decimal_odds DECIMAL(10,2), fractional_odds VARCHAR(20), timestamp TIMESTAMP NOT NULL, created_at TIMESTAMP DEFAULT NOW(). Add composite index on (race_id, dog_id, timestamp) for efficient odds history queries.

Use IF NOT EXISTS for idempotent migrations.</action>
  <verify>cat src/storage/schema.sql shows all three CREATE TABLE statements with indexes</verify>
  <done>schema.sql created with races, dogs, odds tables, foreign keys, and indexes</done>
</task>

<task type="auto">
  <name>Task 3: Create migration helper</name>
  <files>src/storage/migrations.py</files>
  <action>Create migrations.py with run_migrations() function that: reads DATABASE_URL from environment, connects to PostgreSQL using psycopg2, reads schema.sql file, executes it using cursor.execute() with error handling, commits transaction, closes connection. Add logging to print migration status. Include try/except for psycopg2.Error with rollback on failure. Add main guard so it can be run as python -m src.storage.migrations. DO NOT run automatically - user will run when database is ready.</action>
  <verify>cat src/storage/migrations.py shows run_migrations() function with error handling</verify>
  <done>migrations.py created with schema execution, error handling, and logging</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three model files exist and import without errors
- [ ] schema.sql contains CREATE TABLE statements for all tables
- [ ] Foreign key relationships defined correctly
- [ ] Indexes created for performance
- [ ] migrations.py can read and execute schema.sql
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Data models match schema definitions
- Schema supports both daily stats and real-time odds requirements
- Migration system ready (not yet executed - needs actual database)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Data Models & Schema Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- Created Python dataclasses for Race, Dog, and Odds entities
- Designed PostgreSQL schema with proper relationships and indexes
- Built migration system for schema deployment
- Established JSONB storage for flexible dog statistics

## Files Created/Modified

- `src/models/race.py` - Race dataclass with time-based fields
- `src/models/dog.py` - Dog dataclass with stats JSONB
- `src/models/odds.py` - Odds dataclass for bookmaker tracking
- `src/storage/schema.sql` - PostgreSQL DDL with FK constraints
- `src/storage/migrations.py` - Schema execution helper

## Decisions Made

- JSONB for dog stats (flexibility for different stat types from website)
- Composite index on (race_id, dog_id, timestamp) for odds history
- CASCADE delete for referential integrity (delete race → delete dogs & odds)
- VARCHAR primary keys (greyhoundstats.co.uk may use non-numeric IDs)

## Issues Encountered

[Document any schema design challenges or deviations]

## Next Step

Ready for 01-03-PLAN.md (Base Scraper Infrastructure) - can run in parallel with database setup
</output>
