---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-01"]
files_modified: [src/scrapers/base_scraper.py, src/utils/rate_limiter.py, src/utils/stealth.py, src/utils/retry.py]
---

<objective>
Build reusable scraper infrastructure with rate limiting, stealth, and retry logic.

Purpose: Implement the anti-detection and reliability patterns from research so they can be reused by both stats and odds scrapers. Avoid hand-rolling solutions that research warned against.
Output: Base scraper class with Playwright + stealth configuration, rate limiter using token bucket, and retry decorator with exponential backoff.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**From RESEARCH.md "Don't Hand-Roll":**
- Anti-bot evasion: Use playwright-stealth (NOT custom header randomization)
- Rate limiting: Use token bucket algorithm (NOT simple time.sleep)
- Retry logic: Use tenacity library (NOT manual try/catch loops)

**From RESEARCH.md Code Examples:**
- Playwright setup with stealth_async
- Token bucket rate limiter pattern
- Exponential backoff with jitter

**Key constraints:**
- Must respect rate limits (2-5 second delays per request)
- Must use stealth plugins to avoid detection
- Must handle errors gracefully with retry
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rate limiter with token bucket</name>
  <files>src/utils/rate_limiter.py</files>
  <action>Implement TokenBucketRateLimiter class exactly as shown in research code examples. Class takes rate (tokens/second, default 0.5 for 2-sec delays) and capacity (burst, default 5). Maintain tokens counter, last_update timestamp. acquire() method: calculates elapsed time, adds tokens (min of capacity), waits if insufficient tokens, consumes 1 token when ready. Use asyncio.sleep() for async compatibility. Add docstring explaining token bucket pattern. This is NOT hand-rolled - it's the industry standard pattern from research.</action>
  <verify>python -c "from src.utils.rate_limiter import TokenBucketRateLimiter; rl = TokenBucketRateLimiter(); print('Rate limiter imported')"</verify>
  <done>TokenBucketRateLimiter class created with acquire() method, token refill logic, and async support</done>
</task>

<task type="auto">
  <name>Task 2: Create stealth configuration helper</name>
  <files>src/utils/stealth.py</files>
  <action>Create async setup_stealth_page() function that takes Playwright page object and applies stealth. Import playwright_stealth. Apply stealth_async(page) to patch detection vectors. Set realistic viewport (1920x1080), user agent (Mozilla/5.0 Windows NT 10.0 Win64 x64 Chrome/120.0), additional headers (Accept-Language: en-US). Add disable_blink_features argument to launch options: ['--disable-blink-features=AutomationControlled']. Return configured page. DO NOT create custom fingerprint logic - playwright-stealth handles this (research warning: "Modern anti-bot systems analyze dozens of signals").</action>
  <verify>python -c "from src.utils.stealth import setup_stealth_page; print('Stealth helper imported')"</verify>
  <done>setup_stealth_page() function created using playwright-stealth library, not custom fingerprinting</done>
</task>

<task type="auto">
  <name>Task 3: Create base scraper with Playwright</name>
  <files>src/scrapers/base_scraper.py</files>
  <action>Create BaseScraper abstract class using Python ABC. Constructor takes url (str) and rate_limiter (TokenBucketRateLimiter). Include async scrape() method using async context manager pattern from research: launch playwright browser with headless=True and args=['--disable-blink-features=AutomationControlled'], create context with realistic viewport/user-agent, create page, apply setup_stealth_page(), await rate_limiter.acquire() BEFORE navigation (respects rate limits), goto URL with wait_until='networkidle', get page.content(), close browser, return HTML. Use tenacity @retry decorator on scrape() with exponential backoff (wait_exponential(min=2, max=10), stop_after_attempt(3), retry on Exception). Import from tenacity library (research: "don't hand-roll retry logic"). Add abstract parse() method for subclasses to implement. Subclasses will call scrape() then parse().</action>
  <verify>python -c "from src.scrapers.base_scraper import BaseScraper; print('Base scraper imported')"</verify>
  <done>BaseScraper class created with Playwright setup, stealth, rate limiting, retry logic, and abstract parse() method</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from src.utils.rate_limiter import TokenBucketRateLimiter"` succeeds
- [ ] `python -c "from src.utils.stealth import setup_stealth_page"` succeeds
- [ ] `python -c "from src.scrapers.base_scraper import BaseScraper"` succeeds
- [ ] BaseScraper uses tenacity for retry (not manual loops)
- [ ] BaseScraper applies playwright-stealth (not custom headers)
- [ ] Rate limiter uses token bucket (not time.sleep)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hand-rolled solutions for items flagged in research
- Scraper infrastructure follows research patterns exactly
- Code is reusable for both stats and odds scrapers (Phase 2 & 3)
- Phase 1 complete: Foundation ready for scraper implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Base Scraper Infrastructure Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- Implemented token bucket rate limiter for respectful scraping
- Created Playwright stealth configuration helper
- Built base scraper class with retry logic and anti-detection
- Established reusable patterns for Phase 2 & 3 scrapers

## Files Created/Modified

- `src/utils/rate_limiter.py` - Token bucket algorithm for rate limiting
- `src/utils/stealth.py` - Playwright stealth configuration
- `src/scrapers/base_scraper.py` - Abstract base scraper with Playwright
- All using researched libraries (playwright-stealth, tenacity)

## Decisions Made

- Token bucket with 0.5 tokens/sec (2-second delays between requests)
- Tenacity retry: 3 attempts, exponential backoff 2-10 seconds
- Headless browser with stealth patches for all scrapers
- Abstract parse() method for subclass flexibility

## Issues Encountered

[Document any import errors, async issues, or deviations]

## Next Phase Readiness

**Phase 1 Complete - Foundation established**

Ready for Phase 2: Stats Scraper
- BaseScraper can be subclassed for greyhoundstats.co.uk
- Rate limiting and stealth automatically applied
- Data models and schema ready for storage
- All research recommendations implemented

**Blockers:**
- Database not yet deployed (needs PostgreSQL instance)
- DATABASE_URL not yet configured in .env

**Recommendations for Phase 2:**
- Set up free PostgreSQL instance (Neon or Supabase) before starting
- Run migrations.py to create schema
- Test database connection before implementing scraper logic
</output>
