---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [requirements.txt, .env.example, .gitignore, src/__init__.py, README.md]
---

<objective>
Set up Python project structure with scraping dependencies and database configuration.

Purpose: Establish the foundational project structure and install all required libraries identified in research (Playwright, PostgreSQL, stealth plugins).
Output: Working Python environment with all dependencies installed, database connection configured, and project structure ready for development.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

**Constraint from research:** Free hosting budget, PostgreSQL free tier (Neon or Supabase recommended)
**Standard stack from research:** Playwright 1.57.0, Python 3.10+, playwright-stealth, PostgreSQL 14+
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project structure</name>
  <files>src/__init__.py, src/scrapers/__init__.py, src/models/__init__.py, src/storage/__init__.py, src/utils/__init__.py, README.md, .gitignore</files>
  <action>Create standard Python package structure with src/ layout. Create subdirectories: scrapers/ (scraper implementations), models/ (data models), storage/ (database layer), utils/ (helpers for rate limiting, stealth, retry). Add .gitignore with Python standard ignores (*.pyc, __pycache__/, .env, venv/, .pytest_cache/). Create README.md with project title "Greyhound Racing Value Finder" and basic description. Initialize all __init__.py files as empty (makes them importable packages).</action>
  <verify>ls -la src/ shows all subdirectories, git status shows .gitignore is working (no __pycache__ or .pyc files tracked)</verify>
  <done>Project structure created with src/, subdirectories present, .gitignore working, README exists</done>
</task>

<task type="auto">
  <name>Task 2: Install Python dependencies</name>
  <files>requirements.txt</files>
  <action>Create requirements.txt with exact versions from research: playwright==1.48.0 (latest stable), playwright-stealth (latest), beautifulsoup4>=4.12.0, psycopg2-binary>=2.9.0 (PostgreSQL adapter), python-dotenv>=1.0.0 (env management). Also add: aiohttp>=3.9.0 (async HTTP), tenacity>=8.0.0 (retry logic from research "don't hand-roll retry"). Do NOT install yet - just create the file. Installation will be verified separately to check for errors.</action>
  <verify>cat requirements.txt shows all packages listed</verify>
  <done>requirements.txt created with all required packages and versions</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Install Python dependencies and Playwright browsers</action>
  <instructions>
    I've created requirements.txt with all necessary packages from the research.

    Please run these commands:
    1. Create virtual environment: python -m venv venv
    2. Activate it:
       - Windows: venv\Scripts\activate
       - Mac/Linux: source venv/bin/activate
    3. Install packages: pip install -r requirements.txt
    4. Install Playwright browsers: playwright install chromium

    This requires human action because:
    - Virtual environment creation is project-specific
    - pip install may require resolving dependency conflicts
    - Playwright browser download (~400MB) needs confirmation
  </instructions>
  <verification>After installation, I'll verify: pip list shows playwright, python -c "import playwright" succeeds, playwright --version works</verification>
  <resume-signal>Type "done" when installation complete, or describe any errors</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create database configuration</name>
  <files>.env.example, src/storage/db.py</files>
  <action>Create .env.example with DATABASE_URL placeholder (format: postgresql://user:password@host:port/dbname) and comment explaining to copy to .env. Create src/storage/db.py with psycopg2 connection pooling (from research code example). Include get_connection() function that reads DATABASE_URL from environment using python-dotenv, creates ThreadedConnectionPool with minconn=1, maxconn=10. Add execute_query(query, params) helper for safe parameterized queries. Use context manager pattern for connection management. DO NOT hard-code credentials - must read from .env.</action>
  <verify>cat .env.example shows DATABASE_URL template, cat src/storage/db.py shows connection pooling code</verify>
  <done>.env.example created, db.py implements connection pooling with environment variable configuration</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ls src/` shows scrapers/, models/, storage/, utils/ directories
- [ ] `cat requirements.txt` shows all required packages
- [ ] Virtual environment exists and packages installed
- [ ] `python -c "import playwright"` succeeds
- [ ] `playwright --version` returns version number
- [ ] .env.example exists with DATABASE_URL template
- [ ] src/storage/db.py exists with connection pooling
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Project structure follows research recommendations
- Dependencies installed without errors
- Database configuration ready (not yet connected - that's in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Project Setup & Dependencies Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- Created Python project structure with src/ layout
- Installed Playwright, BeautifulSoup, psycopg2, and supporting libraries
- Configured database connection layer with connection pooling
- Established .env-based configuration pattern

## Files Created/Modified

- `src/` - Package structure (scrapers, models, storage, utils)
- `requirements.txt` - Python dependencies with versions
- `.env.example` - Database configuration template
- `src/storage/db.py` - PostgreSQL connection pooling
- `.gitignore` - Python standard ignores
- `README.md` - Project documentation

## Decisions Made

- Virtual environment for dependency isolation
- psycopg2-binary chosen over psycopg2 (binary is simpler for development)
- ThreadedConnectionPool with 1-10 connections (adequate for scraping workload)

## Issues Encountered

[Document any installation issues, dependency conflicts, or deviations]

## Next Step

Ready for 01-02-PLAN.md (Data Models & Schema)
</output>
