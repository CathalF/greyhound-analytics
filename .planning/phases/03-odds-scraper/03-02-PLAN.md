---
phase: 03-odds-scraper
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [src/scrapers/oddschecker_scraper.py, src/storage/race_odds.py, src/storage/dog_matcher.py]
---

<objective>
Implement oddschecker race fixture and odds scraper with dog matching to stats database.

Purpose: Build production scraper that extracts race cards and odds from oddschecker.com, matches dogs to stats database, and stores in races/odds tables.
Output: Working OddscheckerScraper class with race/odds extraction, dog matching logic, and database storage ready for real-time updates.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-odds-scraper/03-01-SUMMARY.md
@.planning/phases/02-stats-scraper/02-03-SUMMARY.md
@src/scrapers/base_scraper.py
@src/models/race.py
@src/models/dog.py
@src/storage/schema.sql
@src/storage/dog_stats.py
@src/utils/rate_limiter.py
@research_notes_oddschecker.md

**From Plan 03-01:**
- Data access strategy: [will be specified in 03-01-SUMMARY.md]
- URL patterns and selectors documented
- Dog name matching strategy selected
- Field mapping defined
- Refresh frequency decided

**From Phase 2:**
- BaseScraper available (Playwright + stealth + rate limit + retry)
- 31+ dogs in stats database with performance data
- Dog stats stored in JSONB with: runs, wins, win_rate, track_stats, distance_stats, recent_form

**Tech stack available:**
- BaseScraper (async Playwright + stealth)
- BeautifulSoup for HTML parsing
- psycopg2 for database
- TokenBucketRateLimiter for rate limiting

**Database schema:**
- `races` table: race_id, track_name, race_time, distance, status
- `dogs` table: dog_id, name, race_id (NULL for stats-only), trap_number (NULL or 1-6), stats JSONB
- `odds` table: odds_id, race_id, dog_id, bookmaker, decimal_odds, fractional_odds, timestamp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OddscheckerScraper class for race fixtures</name>
  <files>src/scrapers/oddschecker_scraper.py</files>
  <action>Create OddscheckerScraper that subclasses BaseScraper (from Phase 1). Implement parse() method using BeautifulSoup (and/or Playwright if JavaScript required) to extract race fixtures based on research_notes_oddschecker.md specifications. Extract fields: race_id (generate from track+time), track_name, race_time (parse into timestamp), distance, list of dogs with their names and trap numbers, list of odds per dog per bookmaker (decimal + fractional). Return structured dict matching Race/Odds model format. Handle missing data gracefully (some races may lack distance, some dogs may lack trap numbers). Include comprehensive docstring. Use async/await pattern consistently. Constructor takes race_url and rate_limiter.</action>
  <verify>python -c "from src.scrapers.oddschecker_scraper import OddscheckerScraper; print('Import successful')" succeeds</verify>
  <done>OddscheckerScraper class implemented with parse() method, handles missing data, returns correct format with race info, dogs, and odds</done>
</task>

<task type="auto">
  <name>Task 2: Create dog name matching logic</name>
  <files>src/storage/dog_matcher.py</files>
  <action>Create dog_matcher.py with function match_dog_to_stats(oddschecker_dog_name) that matches dog names from oddschecker to existing dogs in stats database. Implement the matching strategy chosen in Plan 03-01 (exact-match, fuzzy-match, or normalize-match). Use psycopg2 to query dogs table and find best match. Return (dog_id, confidence) tuple, or (None, 0) if no match. For normalize-match approach: lowercase both names, remove spaces/hyphens/apostrophes, compare normalized strings. Include get_all_dogs_for_matching() helper that returns list of (dog_id, name) from database for matching. Add docstrings explaining matching algorithm and confidence scoring.</action>
  <verify>python -c "from src.storage.dog_matcher import match_dog_to_stats; print('Import successful')" succeeds</verify>
  <done>Dog matching function implemented with chosen strategy, returns dog_id for matches, None for non-matches, handles edge cases</done>
</task>

<task type="auto">
  <name>Task 3: Create database storage layer for races and odds</name>
  <files>src/storage/race_odds.py</files>
  <action>Create race_odds.py with functions for storing races and odds. Implement upsert_race(race_dict) that inserts new race or updates existing (INSERT ... ON CONFLICT on race_id). Implement link_dog_to_race(dog_id, race_id, trap_number) that updates dogs.race_id and dogs.trap_number. Implement upsert_odds(odds_list) that inserts/updates odds records (handle multiple bookmakers per dog). Use psycopg2 parameterized queries. Include get_race(race_id), get_race_odds(race_id), get_upcoming_races(hours_ahead) for retrieval. Add docstrings. Error handling with try/except for psycopg2.Error. Generate odds_id as f"{race_id}_{dog_id}_{bookmaker}_{timestamp}" to make unique.</action>
  <verify>python -c "from src.storage.race_odds import upsert_race, upsert_odds, link_dog_to_race; print('Import successful')" succeeds</verify>
  <done>Database functions implemented with race/odds upsert, dog linking, retrieval queries, and error handling</done>
</task>

<task type="auto">
  <name>Task 4: Create integration test script</name>
  <files>test_odds_scraper.py</files>
  <action>Create test_odds_scraper.py that tests complete pipeline: (1) Initialize TokenBucketRateLimiter (0.5 rate = 2-sec delays) (2) Create OddscheckerScraper with sample race URL from research_notes_oddschecker.md (3) Run scraper and print extracted race data (track, time, distance, dogs with trap numbers, odds per bookmaker) (4) For each dog, call match_dog_to_stats to find dog_id from stats database (5) Call upsert_race to store race (6) Call link_dog_to_race for each matched dog (7) Call upsert_odds for all odds (8) Retrieve with get_race and verify (9) Print summary: race stored, N dogs matched, N dogs linked, N odds records created. Use asyncio.run() for async execution. Include try/except to display errors clearly.</action>
  <verify>File exists at test_odds_scraper.py</verify>
  <done>Test script created that validates scraping → dog matching → race storage → dog linking → odds storage pipeline</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Oddschecker scraper with race/odds extraction and dog matching to stats database</what-built>
  <how-to-verify>
    1. Ensure PostgreSQL database running and DATABASE_URL in .env
    2. Ensure dog stats populated from Phase 2 (run: py -m src.batch_scraper --limit 10)
    3. Run test: py test_odds_scraper.py
    4. Expected: Script prints race info (track, time, distance), lists dogs with trap numbers, shows odds from multiple bookmakers, matches dogs to stats database, stores race/odds/links successfully
    5. Check for: proper dog name matching (matched dogs show dog_id), race stored in database, odds records created, dogs.race_id updated for matched dogs
    6. Verify rate limiting (should take 2+ seconds)
  </how-to-verify>
  <resume-signal>Type "approved" if test passes, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] OddscheckerScraper class imports without errors
- [ ] dog_matcher.py functions import without errors
- [ ] race_odds.py functions import without errors
- [ ] Test script runs successfully
- [ ] Race data correctly stored in database
- [ ] Dog matching works (found matches for known dogs)
- [ ] Dogs linked to race (race_id and trap_number updated)
- [ ] Odds stored correctly (multiple bookmakers per dog)
- [ ] Rate limiting observed (2-second delays)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User approved test results
- Scraper handles missing data gracefully
- Dog matching finds existing dogs from stats database
- Database upsert works for races and odds
- Ready for batch/refresh implementation (Plan 03-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-odds-scraper/03-02-SUMMARY.md`:

# Phase 3 Plan 2: Odds Scraper Implementation Summary

**[One-liner: scraper with dog matching and race/odds storage complete]**

## Accomplishments

- Implemented OddscheckerScraper subclassing BaseScraper
- Created dog name matching logic linking oddschecker to stats database
- Built database storage layer for races and odds
- Linked dogs to races (race_id and trap_number)
- End-to-end test validates scrape → match → store → link

## Files Created/Modified

- `src/scrapers/oddschecker_scraper.py` - Race fixture and odds scraper
- `src/storage/dog_matcher.py` - Dog name matching logic
- `src/storage/race_odds.py` - Race/odds database storage
- `test_odds_scraper.py` - Integration test script

## Decisions Made

- Dog matching strategy: [implementation details]
- Odds storage approach: [one record per dog per bookmaker per timestamp]
- Race ID generation: [pattern used]

## Issues Encountered

[Any parsing challenges, matching issues, or data quality problems]

## Next Step

Ready for 03-03-PLAN.md (Real-Time Odds Refresh)
</output>
