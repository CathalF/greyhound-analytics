---
phase: 03-odds-scraper
plan: 03
type: execute
depends_on: ["03-02"]
files_modified: [src/odds_refresher.py, .gitignore]
---

<objective>
Create real-time odds refresh job with race discovery and continuous updates.

Purpose: Automate discovery of upcoming races and refresh odds frequently (every few minutes) as race time approaches.
Output: Production-ready refresh job that finds races, scrapes odds, updates database, handles multiple concurrent races, with logging.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-odds-scraper/03-DISCOVERY.md
@.planning/phases/03-odds-scraper/03-01-SUMMARY.md
@.planning/phases/03-odds-scraper/03-02-SUMMARY.md
@.planning/phases/02-stats-scraper/02-03-SUMMARY.md
@src/scrapers/oddschecker_scraper.py
@src/storage/race_odds.py
@src/storage/dog_matcher.py
@src/utils/rate_limiter.py
@research_notes_oddschecker.md

**From Plan 03-01:**
- URL patterns for race listings
- Refresh strategy defined

**From Plan 03-02:**
- OddscheckerScraper working
- Dog matching implemented
- Race/odds storage validated

**From Phase 2:**
- Batch scraper pattern with logging, progress tracking, error handling
- Can adapt similar patterns for odds refresh

**Key requirements from ROADMAP:**
- Refresh frequently (every few minutes) close to race time
- Handle multiple concurrent race updates
- Real-time data for value bet identification

**Constraints:**
- Must respect rate limits (2-5 second delays)
- Must handle failures gracefully (some races may fail to scrape)
- Runnable continuously or on schedule
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create odds refresh orchestrator</name>
  <files>src/odds_refresher.py</files>
  <action>Create odds_refresher.py with async main() function that orchestrates race discovery and odds refresh. Implement: (1) Discover upcoming races from oddschecker listings (parse race URLs for next N hours, e.g., next 4 hours) (2) Create single TokenBucketRateLimiter(rate=0.5) shared across all scrapers (3) For each race URL, create OddscheckerScraper and scrape (4) For each scraped race: call upsert_race, match dogs with match_dog_to_stats, link matched dogs with link_dog_to_race, upsert odds (5) On success: log race updated, track stats (6) On failure: log error with race URL and exception, continue to next race (don't crash) (7) Track stats: total races discovered, successfully updated, failed, matched dogs, linked dogs, odds records created (8) Print summary at end. Use asyncio.gather with return_exceptions=True. Add --hours flag to control lookahead window (default 4 hours). Add --once flag to run once vs continuous loop. If continuous (no --once), sleep for refresh_interval (default 5 minutes) between cycles. Include comprehensive logging with timestamps. Make runnable: python -m src.odds_refresher</action>
  <verify>python -m src.odds_refresher --once --hours 2 runs without crashing, discovers races, updates odds with rate limiting</verify>
  <done>Odds refresher discovers races, scrapes odds, matches dogs, updates database, continues on errors, respects rate limits, logs results</done>
</task>

<task type="auto">
  <name>Task 2: Add structured logging</name>
  <files>src/odds_refresher.py</files>
  <action>Enhance odds_refresher.py with Python logging module (not print statements). Configure logging to: (1) Write to logs/odds_refresher.log with daily rotation (2) Format: timestamp, level, message (3) Log levels: INFO for progress (races discovered, updated), WARNING for dog match failures, ERROR for scraping failures (4) Console output: INFO and above (5) File output: DEBUG and above. Create logs/ directory if doesn't exist. Use RotatingFileHandler (10MB max, 5 backups). Ensure logs/ already in .gitignore from Phase 2. Log: refresh cycle start/end, races discovered, each race processed, dog matching results (N matched, N unmatched), odds update success/failure, final summary statistics.</action>
  <verify>logs/odds_refresher.log exists after run with structured entries, shows timestamps and levels</verify>
  <done>Structured logging implemented with file rotation, appropriate levels, clear visibility into refresh process</done>
</task>

<task type="auto">
  <name>Task 3: Add race status tracking</name>
  <files>src/odds_refresher.py, src/storage/race_odds.py</files>
  <action>Add race status tracking to manage race lifecycle. In race_odds.py, add update_race_status(race_id, status) function that updates races.status field. In odds_refresher.py: (1) When race discovered: set status='upcoming' (2) When scraping race within 30 minutes of race_time: set status='imminent' (3) After race_time passes: set status='complete' (stop refreshing this race) (4) Add get_active_races() to race_odds.py that returns races with status in ('upcoming', 'imminent') for refresh targeting. This prevents wasting resources on completed races. Add --include-completed flag to refresh completed races too (for testing). Log status transitions.</action>
  <verify>Database shows races with status field populated, completed races excluded from refresh (unless --include-completed used)</verify>
  <done>Race status tracking prevents refreshing completed races, focuses effort on active races, logs lifecycle transitions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Odds refresher discovers upcoming races
- [ ] Rate limiting respected (2-second delays observed)
- [ ] Failures don't crash entire refresh cycle
- [ ] Logs written to logs/odds_refresher.log
- [ ] Race status tracking excludes completed races
- [ ] --once flag works (single run)
- [ ] --hours flag controls lookahead window
- [ ] Continuous mode (no --once) runs refresh loop
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Odds refresher ready for production use
- Error handling robust (continues on failures)
- Logging provides clear visibility
- Race status tracking prevents wasted work
- Phase 3 complete: Race fixtures and odds operational
</success_criteria>

<output>
After completion, create `.planning/phases/03-odds-scraper/03-03-SUMMARY.md`:

# Phase 3 Plan 3: Real-Time Odds Refresh Summary

**[One-liner: production odds refresh with race discovery and continuous updates]**

## Accomplishments

- Created odds refresh orchestrator for race discovery and updates
- Implemented structured logging with file rotation
- Added race status tracking to manage lifecycle (upcoming → imminent → complete)
- Built production-ready continuous refresh job

## Files Created/Modified

- `src/odds_refresher.py` - Odds refresh orchestrator
- `src/storage/race_odds.py` - Added update_race_status, get_active_races
- `logs/odds_refresher.log` - Generated by refresh runs

## Decisions Made

- Refresh interval: [X minutes between cycles]
- Lookahead window: [default hours for race discovery]
- Status lifecycle: upcoming → imminent (30min before) → complete (after race time)
- Rate limiting: Shared 2-second limiter across all race scraping

## Issues Encountered

[Any race discovery issues, matching problems, or performance challenges]

## Phase 3 Complete

**What we built:**
- Oddschecker race fixture and odds scraper
- Dog name matching linking odds to stats database
- Database storage for races, odds, and dog-race links
- Real-time odds refresh with race discovery
- Race status tracking and lifecycle management

**Integration with Phase 2:**
- Dogs from oddschecker matched to stats database (31+ dogs)
- Dogs.race_id and dogs.trap_number populated when linked
- Combined stats + odds data ready for value bet analysis

**Ready for Phase 4:**
- Phase 4 will build web dashboard
- Display races with combined stats and odds
- Identify value bets (strong stats vs favorable odds)
- Provide betting recommendations

**Usage:**
```bash
# Run once to discover and update races in next 4 hours
python -m src.odds_refresher --once

# Continuous refresh (every 5 minutes) for next 6 hours of races
python -m src.odds_refresher --hours 6

# Test with completed races included
python -m src.odds_refresher --once --include-completed
```

**Typical workflow:**
1. Morning: Run dog stats batch (Phase 2): `python -m src.batch_scraper`
2. Start odds refresh: `python -m src.odds_refresher` (continuous mode)
3. Refresh runs every 5 minutes, updating odds as race time approaches
4. Phase 4 dashboard will display combined data for value bet identification

**Next Phase:**
Ready for Phase 4: Dashboard & Analysis (web interface)
</output>
